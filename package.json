{
  "name": "scotty-efird-personal-website",
  "version": "2.0.1",
  "private": false,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "^3.4.3",
    "node-sass": "^4.14.1"
  },
  "scripts": {
    "dev": "react-scripts build && cls && react-scripts start",
    "build": "react-scripts build",
    "deploy": "cls && vercel --prod"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
