{
  "name": "scotty-efird-personal-website",
  "version": "0.0.1",
  "private": false,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "yarn build && cp ./now.json ./build/ && cd ./build/ && now --target production"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
